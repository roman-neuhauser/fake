# $FreeBSD$

PORTNAME=	fake
PORTVERSION=	__VERSION__
CATEGORIES=	devel

COMMENT=	Mocks and Stubs For the Command Line
LICENSE=	MIT
MAINTAINER=	neuhauser+fake@sigpipe.cz

HAS_CONFIGURE=	yes
CONFIGURE_ARGS+= --prefix=${PREFIX}
CONFIGURE_ARGS+= --mandir=${MANPREFIX}/man
USES+=		gmake
MAKEFILE=	GNUmakefile
TEST_TARGET=	check

USE_GITHUB=	yes
GH_ACCOUNT=	roman-neuhauser
GH_PROJECT=	${PORTNAME}
GH_TAGNAME=	v${PORTVERSION:S/+/-/g}

BUILD_DEPENDS=	basexy:converters/basexy
RUN_DEPENDS=	basexy:converters/basexy
TEST_DEPENDS=	basexy:converters/basexy
TEST_DEPENDS=	cram:devel/cram

NO_CHECKSUM=	yes

.include <bsd.port.mk>
